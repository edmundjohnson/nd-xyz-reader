<resources>
    <!-- list -->

    <!-- This comment is no longer true - the app bar is bigger everywhere.
         The app bar is small by default, but only for phones in landscape mode.
         For portrait orientation, and larger screens, it is bigger. -->
    <!-- <dimen name="toolbar_height">@dimen/abc_action_bar_default_height_material</dimen> -->
    <dimen name="appbar_height">112dp</dimen>

    <!-- detail -->
    <dimen name="detail_title_text_size">32sp</dimen>
    <dimen name="detail_body_text_size">22sp</dimen>
    <item name="detail_body_line_spacing_multiplier" format="float" type="fraction">1.25</item>
    <!-- TODO: Remove fab_press_translation_z if unused -->
    <dimen name="fab_press_translation_z">6dp</dimen>
    <dimen name="fab_margin">24dp</dimen>

</resources>
